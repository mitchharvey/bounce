# bounce: a terminal game

a simple platformer with mouse controls that runs in your terminal

### requirements:
- cmake
- libncurses

### how to build:
```sh
$ apt install cmake libncurses-dev # or equivalent
$ cd src
$ cmake
$ make
$ ./Bounce
```

### how to play:
1. click
2. you win when you reach the top